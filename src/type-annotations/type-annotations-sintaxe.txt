// What are type annotations?
// Type annotations are a feature in TypeScript 
// that allow developers to explicitly specify
// the types of variables, function parameters,
// return values, and other data structures. By
// providing type information, TypeScript can
// perform static type checking,
// which helps catch errors at compile time rather
// than at runtime. This leads to more robust and
// maintainable code.

